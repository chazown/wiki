# 🐧 Server & Linux Cheatsheet

## 1. 📊 System Resources (모니터링)
서버 상태가 이상할 때 가장 먼저 확인합니다.

### 🔹 CPU 및 메모리
# 실시간 리소스 확인 (가장 기본)
top

# 시각적으로 보기 편한 모니터링 (설치 필요: htop)
htop

# 메모리 사용량 (MB/GB 단위 변환)
free -h

### 🔹 디스크 용량
# 전체 디스크 용량 확인
df -h

# 현재 폴더 내 하위 폴더별 용량 계산
du -sh *

# 특정 디렉토리 용량 확인
du -sh /var/log

# 디스크 파티션 및 마운트 정보 (하드웨어)
lsblk

---

## 2. 👥 User Management (사용자 관리)
서버에 새로운 사람을 추가하거나 암호를 변경합니다.

### 🔹 계정 관리
# 새 사용자 생성 (홈 디렉토리 포함)
sudo useradd -m [유저명]

# 비밀번호 변경 (본인 또는 타인)
sudo passwd [유저명]

# 사용자 삭제 (홈 디렉토리까지 깨끗하게)
sudo userdel -r [유저명]

# 내 계정 정보 확인
whoami

### 🔹 권한 부여
# 사용자를 sudo(관리자) 그룹에 추가 - Ubuntu
sudo usermod -aG sudo [유저명]

# 사용자를 wheel(관리자) 그룹에 추가 - CentOS/RHEL
sudo usermod -aG wheel [유저명]

---

## 3. ⚙️ Process Management (프로세스 & Kill)
실행 중인 프로그램을 찾고, 멈추고, 죽이는 다양한 방법입니다.

### 🔹 프로세스 검색
# 모든 프로세스 리스트 확인
ps aux

# 특정 프로세스 검색 (예: python이 들어간 것)
ps aux | grep python

### 🔹 프로세스 종료 (Kill)
# [권장] 정상 종료 요청 (SIGTERM - 작업 정리 후 종료)
kill -15 [PID]

# [주의] 강제 종료 (SIGKILL - 즉시 사살, 데이터 손실 위험)
kill -9 [PID]

# 프로세스 생존 여부 확인 (실제로 종료하지 않음 - 에러 없으면 살아있는 것)
kill -0 [PID]

# 프로세스 이름으로 모두 종료 (예: 모든 nginx 프로세스 종료)
killall nginx

# 이름이 포함된 프로세스 종료 (패턴 매칭)
pkill -f [프로세스명]

### 🔹 백그라운드 작업
# 실행 중인 프로그램을 백그라운드로 보내기 (Ctrl+Z 후)
bg

# 백그라운드 작업을 다시 앞으로 가져오기
fg

---

## 4. 📂 File, Link & Compression (파일 관리)

### 🔹 권한(Chmod) & 소유권(Chown)
# 실행 권한 부여
chmod +x script.sh

# 일반적인 웹서버 파일 권한 (755)
chmod 755 filename

# 소유자 변경 (사용자:그룹)
sudo chown user:group filename
sudo chown -R user:group directory/

### 🔹 바로가기 (Symbolic Link)
설정 파일(nginx 등) 관리 시 필수입니다.
# 바로가기 생성: ln -s [원본파일] [바로가기이름]
ln -s /etc/nginx/sites-available/app /etc/nginx/sites-enabled/

# 링크 확인
ls -l

### 🔹 파일 찾기 & 압축
# 이름으로 파일 찾기
find . -name "*.log"

# 파일 내용 검색 (가장 많이 씀)
grep -r "error" .

# [추가] 파일 무결성 확인 (Checksum)
sha256sum [파일명]
# md5sum도 동일하게 사용 가능

# 압축하기 (tar.gz) / 풀기
tar -czvf backup.tar.gz [폴더명]
tar -xzvf backup.tar.gz

---

## 5. 🌐 Network & Transfer (네트워크 & 전송)

### 🔹 연결 상태 확인
# 외부와 통신 확인 (Ping)
ping google.com

# 내 IP 주소 확인
hostname -I
# 또는
ip addr show

# 도메인 주소 확인 (DNS)
nslookup google.com

### 🔹 파일 전송 (SCP)
# [내 PC -> 서버] 파일 보내기
scp [내파일] [계정]@[서버IP]:[도착경로]

# [서버 -> 내 PC] 파일 가져오기
scp [계정]@[서버IP]:[서버파일] [내도착경로]

### 🔹 다운로드 및 요청
# 파일 다운로드
wget [파일URL]

# 웹페이지 요청/API 테스트 (헤더만 확인)
curl -I [URL]

### 🔹 포트 확인
# 열려있는 포트 확인
netstat -tulpn

---

## 6. 📦 Package Management (패키지 설치)

### 🔹 Ubuntu / Debian (APT)
# 업데이트 및 업그레이드
sudo apt update && sudo apt upgrade -y

# 설치
sudo apt install [패키지명]

# 삭제 (설정파일 포함)
sudo apt purge [패키지명]

### 🔹 RedHat / CentOS / Rocky (DNF/YUM)
# 전체 업데이트
sudo dnf update -y

# 설치
sudo dnf install [패키지명]

# 삭제
sudo dnf remove [패키지명]

---

## 7. 📜 System Logs (저널 로그)
`tail -f` 외에 시스템 관리 데몬(Systemd)의 로그를 확인합니다.

### 🔹 Journalctl 사용법
# 전체 로그 실시간 확인 (최신순)
journalctl -f

# 특정 서비스의 로그만 보기 (예: nginx)
journalctl -u nginx -f

# 부팅 이후 발생한 에러 로그만 보기
journalctl -p 3 -xb

---

## 8. 🐳 Docker Management

### 🔹 컨테이너 관리
# 실행 중인 컨테이너 확인
docker ps

# 컨테이너 끄기/켜기
docker stop [컨테이너명]
docker start [컨테이너명]

### 🔹 접속 및 로그
# 컨테이너 내부 쉘 접속
docker exec -it [컨테이너명] /bin/bash

# 로그 실시간 확인
docker logs -f [컨테이너명]

### 🔹 청소
# 사용 안 하는 컨테이너/이미지 일괄 삭제
docker system prune -a

---

## 9. 🔌 System Control (시스템 제어)

### 🔹 전원 관리
# 시스템 재부팅
sudo reboot

# 시스템 종료 (즉시)
sudo shutdown -h now

### 🔹 시스템 시간 관리
# 시간 설정 확인 및 동기화 상태
timedatectl

# 시간대 변경 (예: 아시아/서울)
sudo timedatectl set-timezone Asia/Seoul

### 🔹 OS 및 커널 정보
# 통합 OS 버전 확인
cat /etc/os-release

# RedHat/CentOS 계열 전용 버전 확인
cat /etc/redhat-release

# 커널 버전 확인
uname -r

# 가동 시간 확인
uptime